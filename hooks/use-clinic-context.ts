// Convenience export for consistent import paths
// Enables: import { useClinicContext } from '@/hooks/use-clinic-context'
// Instead of: import { useClinicContext } from '@/context/clinic-context'

export { useClinicContext } from '@/context/clinic-context'
